export interface FileGroup {
  pathInterface: any;
  isValid: boolean;
  type: string;
  fileUrl: string;
}

export interface HackathonRegistrationDetail {
  semesters: number[];
  imageUrl: string;
}
